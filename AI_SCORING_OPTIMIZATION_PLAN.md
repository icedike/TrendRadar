# TrendRadar AI 评分系统优化方案

## 文档信息
- **创建日期**: 2025-11-18
- **目标**: 让 AI 的聚类和评分结果真正参与新闻排序和统计
- **影响范围**: 数据流重构、评分算法、报表生成

---

## 一、为什么需要优化

### 1.1 当前系统的问题

#### 问题1：AI 的工作成果被浪费
**现状：**
- AI 已经实现了智能聚类功能 (`cluster_events`)，能识别"标题不同但内容相同"的新闻
- AI 已经为每个事件评估了 `importance` (1-10) 和 `confidence` (0-1)

**但是：**
- ❌ 主流程仍使用**标题字符串精确匹配**来去重
- ❌ AI 的聚类结果只用于 HTML 的"AI 热点分析"区域展示
- ❌ importance 和 confidence **完全不影响新闻排序**

#### 问题2：频率统计不准确
**示例场景：**

假设 RSS 收到以下 3 篇新闻：
1. "Mt Gox Moves 10K Bitcoin"
2. "Mt. Gox transfers 10,000 BTC"
3. "10000 BTC Transferred from Mt Gox Wallet"

**AI 识别结果：**
- ✅ 这是同一个事件（Mt Gox 转账）
- ✅ articles = [文章1, 文章2, 文章3]
- ✅ importance = 8.5/10

**主流程统计结果：**
- ❌ 3 条独立的新闻
- ❌ 每条频率 = 1
- ❌ 可能都排在后面（因为频率低）

**用户看到的：**
- 热门榜单上看到 3 条几乎一样的新闻
- 真正重要的事件被拆散，排名下降

#### 问题3：评分系统只考虑机械指标

**现有评分公式** (main.py:1074-1107)：
```python
total_weight = (
    rank_weight * 0.6        # RSS 位置
    + frequency_weight * 0.3  # 出现次数（基于标题去重）
    + hotness_weight * 0.1    # 高排名占比
)
```

**缺失的关键信息：**
- ❌ 无法理解新闻内容的实质重要性
- ❌ "SEC 批准 BTC ETF" 和 "某小币涨5%" 权重可能一样
- ❌ 不考虑监管影响、市场影响、时效性

**AI 已经评估的信息：**
- ✅ market impact（市场影响）
- ✅ regulatory weight（监管重要性）
- ✅ time-sensitivity（时效性）
- ✅ unique sources（来源多样性）

---

### 1.2 数据流断裂示意图

**当前架构：**
```
┌─────────────┐
│ RSS 爬取    │
└──────┬──────┘
       │
       ▼
  results{title: data}  ◄─── 基于标题字符串去重
       │
       ├──────────────────────────┐
       │                          │
       ▼                          ▼
 count_word_frequency      AI 分析 cluster_events
 （标题作为key）           （语义聚类，识别重复）
       │                          │
       ▼                          ▼
   主报表统计                AI 热点分析区域
 （频率不准确）              （孤立展示）
       │                          │
       └────────┬─────────────────┘
                ▼
          HTML 输出
    （主榜单 vs AI区域数据不一致）
```

**问题所在：**
- 主流程和 AI 分析是**两条平行线**
- AI 的聚类结果没有反馈到主流程
- 用户在主榜单看到的数据和 AI 分析区域的数据互相矛盾

---

## 二、优化目标

### 2.1 核心目标
1. **让 AI 聚类结果成为主数据源**
   - 用"事件"替代"标题"作为统计单位
   - 频率 = 同一事件包含的文章数

2. **AI 评分参与排序权重**
   - importance 和 confidence 加入综合评分公式
   - 语义重要性和机械指标结合

3. **统一数据流**
   - 主报表和 AI 分析使用同一份聚类数据
   - 消除数据不一致问题

### 2.2 预期效果

**优化前：**
- Mt Gox 转账新闻被拆成 3 条，每条频率=1，排名靠后
- "某小币涨5%"因为在RSS第1位，排名很高

**优化后：**
- Mt Gox 转账被识别为 1 个事件，频率=3，importance=8.5
- 综合得分考虑 AI 评估的市场影响，排名提升
- "某小币涨5%"因为 importance=2.5，权重下降

---

## 三、优化步骤

### 阶段一：数据流重构（核心改动）

#### Step 1: 修改数据结构
**位置**: main.py 中的数据爬取和处理流程

**改动前：**
```python
results = {
    "rss_source_1": {
        "标题A": {"ranks": [1], "url": "..."},
        "标题B": {"ranks": [3], "url": "..."},
    }
}
```

**改动后：**
```python
results = {
    "rss_source_1": {
        "event_id_1": {
            "event_title": "AI聚合后的标题",
            "articles": [
                {"original_title": "标题A", "rank": 1, "url": "..."},
                {"original_title": "标题A'", "rank": 2, "url": "..."}
            ],
            "frequency": 2,  # 该事件的文章数
            "first_time": "08:00",
            "last_time": "10:00",
            "importance": 8.5,
            "confidence": 0.95,
            "theme": "regulation",
            "subcategory": "etf"
        }
    }
}
```

#### Step 2: 调整 AI 分析调用时机
**当前时机**: 数据统计完成后才调用 AI
**问题**: AI 的聚类结果无法影响主流程统计

**改为**:
```python
def run_crawler():
    # 1. RSS 爬取原始数据
    raw_results = fetch_rss_feeds()

    # 2. 如果 AI 启用，先进行聚类
    if ai_enabled:
        event_based_results = ai_analyzer.cluster_and_enrich(raw_results)
    else:
        # 降级：基于标题去重
        event_based_results = fallback_title_dedup(raw_results)

    # 3. 基于事件进行频率统计
    stats = count_event_frequency(event_based_results)

    # 4. 生成报表
    generate_reports(stats)
```

#### Step 3: 新增 `cluster_and_enrich` 方法
**位置**: ai_analyzer.py

**功能**:
```python
def cluster_and_enrich(self, raw_results, title_info):
    """
    将原始的标题字典转换为事件字典

    输入: raw_results = {source_id: {title: data}}
    输出: event_results = {source_id: {event_id: event_data}}

    处理流程:
    1. 准备 AI payload（所有文章列表）
    2. 调用 cluster_events 进行聚类
    3. 为每个事件调用 classify、score_importance、sentiment
    4. 计算事件的频率、首次/最后时间
    5. 返回事件字典
    """
```

#### Step 4: 兼容降级模式
**场景**: AI 不可用时（Ollama 未启动、网络问题）

**降级策略**:
```python
def fallback_title_dedup(raw_results):
    """
    基于标题归一化的简单去重
    将相似标题（去除标点、大小写后相同）归为一个事件
    """
    # 模拟事件结构，但不包含 importance/confidence
    # 后续评分只使用机械指标
```

---

### 阶段二：评分算法增强

#### Step 5: 扩展权重配置
**位置**: config/config.yaml

**新增配置**:
```yaml
weight:
  # 原有权重（AI不可用时使用）
  rank_weight: 0.6
  frequency_weight: 0.3
  hotness_weight: 0.1

  # AI 增强权重（AI可用时使用）
  ai_enhanced:
    enabled: true  # 是否启用AI增强评分
    rank_weight: 0.3          # 降低排名权重
    frequency_weight: 0.25     # 降低频率权重
    hotness_weight: 0.05       # 降低热度权重
    importance_weight: 0.3     # 新增：AI重要性权重
    confidence_weight: 0.1     # 新增：AI置信度权重
```

**设计说明**:
- `importance_weight`: AI 评估的实质重要性（市场影响、监管影响、时效性）
- `confidence_weight`: AI 聚类的置信度（来源可靠性、事件确定性）
- 保留原有权重作为降级方案

#### Step 6: 修改评分函数
**位置**: main.py:1074

**新增逻辑**:
```python
def calculate_news_weight(event_data: Dict, rank_threshold: int) -> float:
    """计算事件权重，用于排序"""

    # 检查是否有 AI 评分
    has_ai_score = "importance" in event_data and "confidence" in event_data

    if has_ai_score and CONFIG["WEIGHT_CONFIG"].get("ai_enhanced", {}).get("enabled", False):
        # AI 增强模式
        weights = CONFIG["WEIGHT_CONFIG"]["ai_enhanced"]

        # 基础指标
        rank_weight = calculate_rank_weight(event_data["ranks"])
        frequency_weight = min(event_data["frequency"], 10) * 10
        hotness_weight = calculate_hotness(event_data["ranks"], rank_threshold)

        # AI 指标
        importance = event_data["importance"]  # 1-10
        confidence = event_data["confidence"]  # 0-1

        # 归一化 importance 到 0-100 范围
        importance_weight = importance * 10
        confidence_weight = confidence * 100

        total_weight = (
            rank_weight * weights["rank_weight"]
            + frequency_weight * weights["frequency_weight"]
            + hotness_weight * weights["hotness_weight"]
            + importance_weight * weights["importance_weight"]
            + confidence_weight * weights["confidence_weight"]
        )
    else:
        # 传统模式（兼容无AI场景）
        weights = CONFIG["WEIGHT_CONFIG"]
        total_weight = (
            rank_weight * weights["rank_weight"]
            + frequency_weight * weights["frequency_weight"]
            + hotness_weight * weights["hotness_weight"]
        )

    return total_weight
```

---

### 阶段三：报表生成调整

#### Step 7: 修改 HTML 报表模板
**位置**: main.py 生成 HTML 的部分

**改动点**:
1. **主榜单展示事件而非标题**
   - 显示聚合后的事件标题
   - 频率显示为"包含X篇文章"
   - 点击可展开查看原始标题列表

2. **新增 AI 评分展示**（如果有）
   ```html
   <div class="news-item">
       <h3>Mt. Gox 转账 10,000 BTC</h3>
       <span class="frequency">包含 3 篇文章</span>
       <span class="ai-score">重要性: 8.5/10 | 置信度: 95%</span>
       <span class="theme">主题: 市场 / 大额转账</span>

       <details>
           <summary>查看原始文章</summary>
           <ul>
               <li>[Blockworks] Mt Gox Moves 10K Bitcoin</li>
               <li>[The Block] Mt. Gox transfers 10,000 BTC</li>
               <li>[Mark金融] Mt Gox 钱包活动检测</li>
           </ul>
       </details>
   </div>
   ```

3. **统一 AI 分析区域**
   - 不再单独显示"AI 热点分析"
   - 或者改为"深度解读"，只展示摘要和分析

#### Step 8: 修改通知推送格式
**位置**: main.py 的各种通知函数（飞书、Telegram等）

**改动**:
- 事件标题 + 频率
- 显示 AI 主题分类和重要性
- 可选：显示包含的来源列表

**示例**:
```
🔥 [重要性 8.5/10] Mt. Gox 转账 10,000 BTC
📊 频率: 3篇 | 主题: 市场/大额转账
📰 来源: Blockworks, The Block, Mark金融
⏰ 08:00 ~ 10:00
```

---

### 阶段四：测试和验证

#### Step 9: 单元测试
**新增测试文件**: tests/test_ai_integration.py

**测试场景**:
1. AI 可用时的聚类和评分
2. AI 不可用时的降级处理
3. 事件频率统计准确性
4. 权重计算公式正确性
5. 新旧数据结构兼容性

#### Step 10: 集成测试
**测试流程**:
1. 准备测试 RSS 数据（包含重复事件）
2. 运行完整流程
3. 验证：
   - HTML 主榜单显示正确的事件和频率
   - AI 评分影响了排序
   - 通知消息格式正确
   - 降级模式工作正常

#### Step 11: 性能测试
**关注点**:
- AI 聚类是否会显著增加处理时间
- 大量文章时的批次处理效率
- 缓存机制是否有效

---

## 四、风险控制

### 4.1 兼容性风险
**风险**: 现有用户的历史数据无法读取

**缓解措施**:
- 保留读取旧格式数据的兼容代码
- 新旧数据自动转换适配器
- 增加版本标识字段

### 4.2 AI 依赖风险
**风险**: Ollama 不可用导致功能降级

**缓解措施**:
- 降级模式使用标题归一化去重
- 明确提示用户当前模式（AI 增强 / 传统模式）
- config 中允许手动禁用 AI 增强评分

### 4.3 性能风险
**风险**: AI 聚类耗时过长

**缓解措施**:
- 保留现有的批次处理逻辑（batch_size=20）
- 充分利用缓存（cache_ttl_hours=24）
- 异步处理 + 进度提示

### 4.4 评分权重风险
**风险**: 新的权重配置可能不符合用户预期

**缓解措施**:
- 提供默认配置 + 可调整参数
- 文档说明各权重的含义和推荐值
- 用户可通过 config 切换传统模式

---

## 五、实施顺序

### Phase 1: 准备工作（预计 1 天）
- [ ] 备份当前代码
- [ ] 创建功能分支 `feature/ai-enhanced-scoring`
- [ ] 编写测试数据集

### Phase 2: 核心重构（预计 2-3 天）
- [ ] Step 1-4: 数据流重构
- [ ] 单元测试验证

### Phase 3: 评分增强（预计 1 天）
- [ ] Step 5-6: 权重配置和评分算法
- [ ] 测试不同权重配置的效果

### Phase 4: UI 调整（预计 1 天）
- [ ] Step 7-8: 报表和通知格式
- [ ] 视觉效果验证

### Phase 5: 测试和优化（预计 1-2 天）
- [ ] Step 9-11: 全面测试
- [ ] 性能优化
- [ ] 文档更新

### Phase 6: 发布（预计 0.5 天）
- [ ] 合并到主分支
- [ ] 更新 README 和配置示例
- [ ] 发布 release notes

---

## 六、成功标准

### 6.1 功能性标准
- ✅ AI 聚类结果用于主榜单统计
- ✅ 相同事件的不同表述被正确归并
- ✅ importance 和 confidence 参与排序
- ✅ AI 不可用时降级模式正常工作

### 6.2 准确性标准
- ✅ 测试集中 95% 的重复事件被正确识别
- ✅ 重要事件（importance>7）排名提升
- ✅ 频率统计准确（误差 <5%）

### 6.3 性能标准
- ✅ 100 篇文章处理时间 <30 秒（AI 模式）
- ✅ 缓存命中时处理时间 <5 秒
- ✅ 降级模式处理时间与现有系统相当

### 6.4 用户体验标准
- ✅ HTML 报表清晰展示事件聚合信息
- ✅ 通知消息包含 AI 评分和来源信息
- ✅ 配置文件易于理解和调整

---

## 七、后续优化方向（可选）

1. **跨时间段事件追踪**
   - 追踪同一事件在多天的演进
   - 识别"持续发酵"的热点

2. **主题聚合视图**
   - 按 AI 分类的主题（regulation, market 等）分组展示
   - 生成主题趋势报告

3. **个性化权重**
   - 允许用户自定义关注的主题
   - 对特定主题的事件提高权重

4. **智能摘要**
   - 利用 AI 生成的 summary 替代简单标题列表
   - 每日汇总邮件使用 AI 摘要

---

## 附录：关键代码位置

| 功能模块 | 文件位置 | 关键函数/类 |
|---------|---------|-----------|
| RSS 爬取 | main.py:650-730 | `RSSFetcher.crawl_feeds()` |
| AI 聚类 | ai_analyzer.py:322-429 | `AIAnalyzer.cluster_events()` |
| AI 评分 | ai_analyzer.py:500-537 | `AIAnalyzer.score_importance()` |
| 频率统计 | main.py:1200-1507 | `count_word_frequency()` |
| 权重计算 | main.py:1074-1107 | `calculate_news_weight()` |
| HTML 生成 | main.py:1769-2800 | `generate_html_report()` |
| 配置管理 | main.py:91-176 | `load_config()` |

---

## 结论

本次优化将实现从"基于标题的机械统计"到"基于 AI 理解的智能分析"的转变，充分发挥 AI 分析的价值，为用户提供更准确、更有洞察力的热点新闻排行。

通过分阶段实施和充分的测试验证，可以在保持系统稳定性的前提下完成升级。
